import { IsEmail, IsNotEmpty, IsOptional, MinLength } from 'class-validator';

export class {{NamePascal}} {
  @IsEmail()
  email: string;

  @IsNotEmpty()
  @MinLength(6)
  password: string;

  @IsOptional()
  device: string;
}
