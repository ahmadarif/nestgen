import { Injectable, NestMiddleware } from '@nestjs/common';

@Injectable()
export class {{NamePascal}}Middleware implements NestMiddleware {
  async use(req: any, res: any, next: any) {
    next();
  }
}
